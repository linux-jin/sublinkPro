[English](CHANGELOG.md) | 简体中文

# 更新日志

这里记录 SublinkPro 的版本更新。README 只保留最新版本摘要，功能级细节放在 `docs/features/` 下。

## [未发布]

### 新增

- 新增独立智能分组：按落地国家跨原有分组筛选节点，不移动或复制节点，同一节点可属于多个智能分组。
- 支持配置延迟、下载速度和测速结果新鲜度条件；最低速度为 0 时只需延迟检测成功；正数速度阈值才要求下载测速成功。
- 可使用现有节点检测配置对所选国家的全部候选节点测速，包括此前失败或未测速的节点。
- 新增智能分组管理、成员预览，并支持在订阅的动态/混合模式中选用智能分组；订阅预览同步生效。

### 调整

- 订阅新增/编辑表单可直接选择智能分组并自动切换动态/混合模式；智能分组卡片支持一键带入新增订阅。订阅列表独立显示已关联智能分组数量，选项加载不再受其他接口失败牵连。
- 智能分组国家列表不再仅限已有节点，补全菲律宾等国家/地区；新增节点名称关键词、原分组筛选和候选数展示，方便定位空结果。
- 最低速度为 0 时允许仅 TCP 延迟检测成功的节点入组；批量填充国家后立即同步节点缓存。
- 智能分组“可用节点”改为分页弹框，未测速、失败及可用节点均可查看逐节点原因；可在弹框内批量或单节点测速。

### 修复

- 修复智能分组页面候选数据尚未加载时，分页组件读取空值导致整页崩溃的问题。
- 智能分组支持“已保存国家 / 名称中的所选国家 / 名称关键词”三者任一命中，即使保存的国家与名称不一致；原分组和测速条件仍需同时满足。名称命中不会覆盖已保存落地国家。
- 增加排除原因统计和“按名称推断”标记，区分无候选、检测失败、阈值限制及结果过期。

## [1.12.0] - 2026 年 9 月 18 日

### 新增

- 新增 SOCKS5 独立账号，每个账号使用单独加密密码并直接绑定路由 Profile。
- 新增多 SOCKS5 监听器，支持稳定 ID、监听器默认 Profile、认证覆盖、聚合监控和带监听器作用域的连接 ID。
- 新增 Loon `.lcf` 模板管理和原生完整配置生成，支持本地节点注入、Remote Filter 展开，并保留 Loon 规则、插件、脚本和 MITM 等 section。
- 新增脱敏公开版 `template/loon.lcf`，不包含私人证书、凭据、订阅、Host 映射或 Wi-Fi SSID。

### 调整

- 提高 README 中本分支新增功能的展示优先级，并明确配置 Loon 模板时优先使用原生输出。
- 未选择原生 Loon 模板时，继续保留 Sub-Store 作为兼容转换路径。
- 自动修复历史 `.lcf` 模板类别，以及过去误把 Loon 模板保存在 Clash 模板字段中的订阅配置。

### 修复

- 补齐节点导入结果弹窗缺失的中英文翻译。

## [1.11.0] - 2026 年 9 月 17 日

### 新增

- SOCKS5 新增多路由配置，每个 Profile 可独立设置候选节点池、选路策略、重试、冷却和粘性会话。
- 新增向后兼容的用户名选路：`username` 使用默认配置，`username@profile` 选择路由配置，`username@profile.account` 增加账号级出口粘性。
- 新增路由配置 CRUD、连接监控 Profile 信息、按 Profile 查看节点负载，以及覆盖所有已启用 Profile 的合并健康探测。

### 修复

- Clash/Mihomo YAML 导入、仅元数据编辑、订阅刷新和再次导出时，保留 `smux` 扩展选项等未建模节点字段。
- 节点延迟与速度测试现在会正确使用其配置的 `DialerProxyName` 前置代理。

### 调整

- 同步上游最新依赖更新，并在 README 中明确本增强分支与 `ZeroDeng01/sublinkPro` 的关系。

## [1.10.0] - 2026 年 9 月 17 日

### 新增

- 新增仅管理员可见的 SOCKS5 节点负载表，支持服务端分页、搜索、健康状态筛选、排序、延迟来源和实时 P2C 评分。
- 新增节点运行统计重置接口和页面操作，可在不中断活动连接的情况下清空逐节点成功、失败和最近选择计数。

## [1.9.0] - 2026 年 9 月 17 日

### 新增

- SOCKS5 新增 `smart` 智能选择策略，使用 P2C、活动连接负载、健康延迟和连续失败惩罚进行调度。
- 新增有界的内存逐节点运行时路由计数，并精确跟踪活动连接生命周期。

## [1.8.0] - 2026 年 9 月 16 日

### 新增

- SOCKS5 新增内存粘性会话，可按客户端 IP 或已认证的 SOCKS5 用户名绑定，并支持可配置的滑动 TTL。
- 粘性节点异常、进入冷却、链接变化或离开候选池时会自动重新选择节点。

## [1.7.0] - 2026 年 9 月 16 日

### 新增

- SOCKS5 新增候选节点池，可按分组、来源、协议和国家/地区组合过滤。
- SOCKS5 指定出站节点支持关键词搜索、分组筛选和服务端分页加载。

### 调整

- SOCKS5 主动健康探测结果开始参与选路；`best` 优先使用探测延迟，并在存在替代节点时跳过探测失败节点。

## [1.6.0] - 2026 年 9 月 15 日

### 新增

- 新增可选的 SOCKS5 节点主动健康探测、逐节点状态与延迟、指数冷却和手动立即探测。
- SOCKS5 监控新增总上下行流量、单连接流量、连接时长和空闲时间。

### 调整

- SOCKS5 停止时会取消仍处于握手阶段的连接。

## [1.5.0] - 2026 年 9 月 15 日

### 新增

- 新增 SOCKS5 全局连接上限和单客户端连接上限配置。
- 新增空闲超时和单连接最大持续时间限制。
- 新增仅管理员可用的 SOCKS5 实时监控，展示活动连接详情、聚合计数和上下行流量字节数。
- 新增管理员断开单条活动连接或全部活动连接的控制。
- 新增 SOCKS5 状态、活动连接列表和连接控制 API。

### 安全

- SOCKS5 连接监控和断开接口均要求管理员认证。
- 非管理员不会获取连接标识和流量详情。

参见 [SOCKS5 网关](docs/features/socks5.zh-CN.md)。

## [1.4.0] - 2026 年 9 月 14 日

### 新增

- SOCKS5 新增轮询路由、可配置重试次数、单节点拨号超时、失败节点冷却和指定节点失败回退。
- 新增 mihomo 适配器池复用，并在节点链接变化和服务生命周期结束时正确失效、清理。

### 性能

- 节点管理接口新增可选紧凑列表投影，减少响应数据体积。
- 减少大规模节点列表的重复请求和不必要渲染。

### 安全

- 非回环地址上的 SOCKS5 监听必须启用认证。

## [1.3.0] - 2026 年 9 月 11 日

### 新增

- 新增管理员可配置的本地 SOCKS5 网关。
- 支持 IPv4、IPv6 和域名目标的 TCP `CONNECT`。
- 支持用户名/密码认证、最佳节点、随机节点和指定节点选择。
- 新增运行时启动/停止控制，以及 **系统设置 → SOCKS5 网关** 侧边栏页面。
- SOCKS5 密码加密保存，并新增管理员专用设置 API。

### 安全

- 网关默认关闭。
- 默认监听地址为 `127.0.0.1:1080`。
- 一期明确不包含 UDP `ASSOCIATE` 和 `BIND`。

参见 [SOCKS5 网关](docs/features/socks5.zh-CN.md)。

## [1.2.21] - 2026 年 9 月 10 日

### 新增

- 新增 WebDAV 定时备份和 5 段 Cron 表达式配置。
- 定时任务复用手动 ZIP 上传流程，在任务中心以 `webdav_backup` 展示进度，并跳过重叠上传。
- 恢复备份时保留当前 WebDAV 连接和定时计划设置。

### 修复

- 创建 WebDAV 目录时使用带尾斜杠的 `MKCOL`，并接受同主机集合重定向，修复 TeraCLOUD 一类服务的上传问题。

参见 [系统备份与 WebDAV](docs/features/backup.zh-CN.md)。

## [1.2.20] - 2026 年 9 月 10 日

### 新增

- 新增仅管理员可用的系统备份设置页。
- 新增 WebDAV 备份 ZIP 上传、远程列表、下载和基于现有数据库迁移流程的恢复。
- 新增加密保存 WebDAV 密码、HTTPS/私有网络安全限制、归档大小限制、ZIP bomb/路径穿越/符号链接防护、重定向阻止、临时文件清理和恢复任务跟踪。

### 修复

- 修复 `/v1/users/me` 返回 `roles: ["ADMIN"]` 时管理员识别失败的问题。

参见 [系统备份与 WebDAV](docs/features/backup.zh-CN.md)。

## [1.2.19] - 2026 年 9 月 9 日

### 新增

- 新增 Clash/mihomo `type: openvpn` 节点导入和导出支持。
- 保留 OpenVPN 传输、加密/认证、凭据、PEM 块、TLS 静态密钥、保活、DNS、IP 栈和 `dialer-proxy` 等字段。
- 使用内部无损格式 `openvpn://server:port?...#name` 支持编辑和往返转换。

### 兼容性

- OpenVPN 节点会输出到 Clash/mihomo；v2ray 和 Surge 等不支持的输出会跳过。
- 当前不包含直接导入 `.ovpn` 文件的能力。

参见 [机场管理](docs/features/airport.zh-CN.md) 和 [订阅分享](docs/features/subscription-share.zh-CN.md) 中的 OpenVPN 说明。

> `v1.2.19` 之前的版本可通过仓库历史和 Git tag 查看。
